services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Persist ChromaDB data across container restarts
      - chroma_data:/app/chroma_db

volumes:
  chroma_data:
